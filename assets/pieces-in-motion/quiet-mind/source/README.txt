Drop the approved source artwork for this project here (PNG/JPG, any size). Then run:
  python3 tools/build-posters.py
It writes optimized 1280x720 poster derivatives (JPEG + WebP) one level up, and leaves this source untouched.
Source files here are served publicly; keep only approved artwork, never masters, boards, or transfer files.
