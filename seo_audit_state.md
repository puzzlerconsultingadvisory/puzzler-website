# SEO Audit State — https://puzzlerconsultingadvisory.com
Started: 2026-09-10T16:55Z

## Categories (status: not_started | in_progress | verified | blocked)
- [x] Technical foundation (crawlability, indexing, robots/sitemap, HTTPS, redirects) — verified (source-level 2026-09-10; HTTP layer verified live 2026-09-11 via the www host)
- [x] Site architecture & internal linking — verified
- [x] Page speed & Core Web Vitals — verified (lab; live compression/TTFB/cache headers confirmed 2026-09-11; field CWV still needs CrUX/PSI)
- [x] Mobile usability — verified (local lab; device-farm and live-URL checks pending)
- [x] On-page SEO (titles, meta, headings, content-keyword alignment) — verified
- [x] Structured data / schema markup — verified
- [x] Content quality & gaps — verified
- [x] Off-page signals (backlinks, domain authority, brand mentions) — blocked (no backlink-tool access; social/profile URLs egress-blocked). Partial evidence recorded.
- [x] Competitive positioning (if competitors provided) — blocked (no competitors supplied). SERP snapshot for two target queries recorded as PENDING context.

## Findings Log
<one entry per finding, appended each loop — never overwritten>

### Loop 1 — Technical foundation (2026-09-10)
PLAN: Check live HTTPS/www/http redirects and headers; robots.txt and sitemap.xml; canonical + robots meta on every HTML page; Vercel rewrites vs canonicals; 404 handling; indexation status.
METHOD NOTE: Live domain is denied by this session's network egress policy (curl, WebFetch: EGRESS_BLOCKED for puzzlerconsultingadvisory.com and www). The site is a no-build static deploy of `main` via Vercel, and this branch has zero diff from `main` (`git diff --stat main..HEAD` empty), so every HTML/robots/sitemap finding below is verified against the exact bytes Vercel serves. HTTP-layer behaviour (redirects, headers, 404 codes) is PENDING VERIFICATION.

[TECHNICAL] [SEVERITY: High] [VERIFIED] — Site appears absent from the search index: `site:puzzlerconsultingadvisory.com` returns zero results and an exact-name search for "Puzzler Consulting & Advisory Services" returns only unrelated "Puzzle Consulting" firms — evidence: WebSearch 2026-09-10 (both queries) — fix: confirm in Google Search Console (add property, submit sitemap, use URL Inspection on `/`); if GSC shows "Discovered, not indexed" the fixes below (sitemap lastmod, internal links, structured data, content depth) are the lever. Confirmation of *cause* is PENDING VERIFICATION (needs GSC access).
[TECHNICAL] [SEVERITY: Medium] [VERIFIED] — robots.txt `Disallow`s /about.html, /services.html, /contact.html while those pages also carry `<meta name="robots" content="noindex,nofollow">`; a Disallow prevents crawlers from ever reading the noindex, so the URLs can still be indexed from external links as title-less entries — evidence: robots.txt:2-4; about.html/services.html/contact.html `<meta name="robots">` — fix: remove the three Disallow lines and rely on noindex (or delete the dormant pages entirely, which IMPLEMENTATION_STATUS.md §C8 already recommends).
[TECHNICAL] [SEVERITY: Medium] [VERIFIED] — Dormant pages declare canonicals to URLs that are not served: `https://puzzlerconsultingadvisory.com/about`, `/services`, `/contact` (no `.html`); vercel.json has no `cleanUrls` and no rewrite for these, so the canonical targets 404 — evidence: about.html/services.html/contact.html `<link rel="canonical">`; vercel.json (only two rewrites) — fix: delete the pages or set canonical to the served URL (moot if deleted).
[TECHNICAL] [SEVERITY: Medium] [VERIFIED] — Clean URLs `/making-the-pieces-fit` and `/capability-brief` are Vercel *rewrites*, not redirects, so `/making-the-pieces-fit.html` and `/capability-brief.html` also return 200 with identical content, and neither page declares a `<link rel="canonical">` → two indexable duplicates per page — evidence: vercel.json rewrites; `grep canonical making-the-pieces-fit.html capability-brief.html` = 0 matches — fix: add `<link rel="canonical" href="https://puzzlerconsultingadvisory.com/making-the-pieces-fit">` (and `/capability-brief`), and add 301 `redirects` in vercel.json from the `.html` forms to the clean forms.
[TECHNICAL] [SEVERITY: Medium] [VERIFIED] — sitemap.xml lists only 3 URLs (/, /privacy.html, /terms.html); the two video pages (linked from the homepage, indexable) and puzzler_card.html are omitted; homepage `lastmod` is 2026-05-13 although index.html was last changed 2026-09-10 (git log) — evidence: sitemap.xml; `git log -1 -- index.html` = 2026-09-10 — fix: add /making-the-pieces-fit and /capability-brief; set homepage lastmod to the real date (2026-09-10) and keep it updated on each content deploy (a 5-line script in tools/ can derive it from git).
[TECHNICAL] [SEVERITY: Low] [VERIFIED] — puzzler_card.html (digital business card with a personal direct email + vCard download) is indexable (self-canonical, no robots meta) but orphaned: no page links to it and it is not in the sitemap. Decide intentionally — evidence: puzzler_card.html head; `grep -r puzzler_card *.html` = only self-reference — fix: add `<meta name="robots" content="noindex">` if it is a QR-code destination only; otherwise link it from the About/Note section and list it in the sitemap.
[TECHNICAL] [SEVERITY: Low] [VERIFIED] — No custom 404 page (`404.html` absent); Vercel will serve its generic 404 with no navigation back to the site — evidence: `ls 404.html` = not found — fix: add a branded `404.html` (Vercel serves it automatically for static projects) with links to `/`, video pages and the Fit Call.
[TECHNICAL] [SEVERITY: Low] [VERIFIED] — Video pages carry `og:type="video.other"` but no `og:url`, no `og:video`, and no Twitter card tags — evidence: `grep -c og:url making-the-pieces-fit.html capability-brief.html` = 0 — fix: add og:url (clean URL), twitter:card=summary_large_image, and for the MP4 page `og:video` + `og:video:type="video/mp4"`.
[TECHNICAL] [SEVERITY: Low] [PENDING VERIFICATION] — HTTPS enforcement, www→apex redirect, HSTS header, and 404 status codes could not be observed (egress blocked). Vercel defaults normally give http→https 301 and a 404 status for unknown paths; www behaviour depends on how the domain was added in the Vercel dashboard — evidence: none obtainable in-session — fix: run `curl -I http://www.puzzlerconsultingadvisory.com/` from any machine and confirm a single 301 to `https://puzzlerconsultingadvisory.com/`; add `Strict-Transport-Security` via vercel.json `headers` if absent.
[TECHNICAL] [SEVERITY: Info] [VERIFIED] — Positives: canonical on `/`, `/privacy.html`, `/terms.html`; `robots.txt` references the sitemap; homepage has no robots meta (defaults to index,follow); single `<h1>`; `lang="en"`; viewport present on every page; html-validate passes clean on all six published pages (`npm run validate`, 0 errors).
REFLECT: Checked robots, sitemap, canonicals, robots meta, rewrites, 404, OG/Twitter, indexation, HTML validity, git recency. What a specialist would still ask for: live headers and redirect chain — genuinely unobtainable here, logged as PENDING. Category marked verified at source level.

### Loop 2 — Site architecture & internal linking (2026-09-10)
PLAN: Map every internal link across all 9 HTML files; check anchor-target integrity, heading hierarchy, link depth, orphan pages, external-link hygiene, and whether the URL structure gives each service topic its own crawlable URL.

[ARCHITECTURE] [SEVERITY: High] [VERIFIED] — The entire offer (five practices, government-contracting sector practice, fractional leadership, speaking, founder bio, method, three videos) lives on one URL, index.html, at ~8,560 words and 156 KB; there are no service, sector, or topic pages. A single URL can carry one primary query intent, so "government contracting readiness", "fractional COO for nonprofits", "nonprofit compliance", "speaking engagements" etc. have no page that can rank for them, and the primary nav is anchor-only (`#practices`, `#method`, `#pieces-in-motion`, `#note`) — evidence: index.html primary-nav hrefs; section ids list; word count 8,561; IMPLEMENTATION_STATUS.md line 203 ("A dedicated government contracting page is agreed for a later pass") — fix: split into a shallow hub-and-spoke: `/services/` (or one page per practice), `/government-contracting/`, `/fractional-leadership/`, `/speaking/`, `/about/`; keep the homepage as the narrative and link each section's heading to its page. Start with government contracting since copy is already approved.
[ARCHITECTURE] [SEVERITY: Medium] [VERIFIED] — Secondary pages are link dead-ends: making-the-pieces-fit.html, capability-brief.html and puzzler_card.html link back only via the brand mark to `https://puzzlerconsultingadvisory.com` (absolute, no path) and to nothing else; privacy/terms link only to `/`, each other and themselves; neither legal page nor either video page links to the other video page — evidence: per-page href extraction (Loop 2 script) — fix: add a shared footer to every page (home, both videos, privacy, terms, Fit Call) so link equity and crawl paths flow both ways.
[ARCHITECTURE] [SEVERITY: Medium] [VERIFIED] — Homepage links to the video pages via the clean rewrite URLs (`/making-the-pieces-fit`, `/capability-brief`) while those pages have no canonical; combined with the Loop 1 rewrite finding, crawlers can reach both the clean and `.html` variants — evidence: index.html hrefs; vercel.json — fix: same as Loop 1 (canonical + 301 redirects).
[ARCHITECTURE] [SEVERITY: Low] [VERIFIED] — Heading hierarchy skips from `<h2>` (section title) to a JS-injected `<h5 id="fysp-result-title">` in the Find Your Starting Point result; every other heading is in order (56 headings scanned) — evidence: index.html line ~1727 (`'<h5 id="fysp-result-title"…'`) — fix: change to `<h3>` (or `<h4>` if the FYSP intro uses h3) and restyle.
[ARCHITECTURE] [SEVERITY: Low] [VERIFIED] — Three dormant pages (about/services/contact) remain deployed but unlinked; their internal nav points to `/about`, `/services`, `/contact` clean URLs that are not rewritten in vercel.json — evidence: about.html nav hrefs; vercel.json — fix: delete them (recommended in IMPLEMENTATION_STATUS.md §C8) or rebuild them as the spoke pages recommended above.
[ARCHITECTURE] [SEVERITY: Info] [VERIFIED] — Positives: all 9 in-page anchor targets resolve to existing ids (0 broken anchors); every page is ≤1 click from home except the intentional orphan card page; 23 external links, none use `target="_blank"` (no tab-hijack), 10 carry `rel="noopener"`; nav has `aria-label`; no links are `nofollow`'d that shouldn't be; 13 Calendly CTAs give strong conversion pathing.
REFLECT: Link graph, anchors, headings, orphaning, URL structure and nav all examined across every file. A specialist might additionally ask for crawl-depth stats from a real crawler; with 9 files and full source access the manual graph is exhaustive. Verified.

### Loop 3 — Page speed & Core Web Vitals (2026-09-10)
PLAN: Lighthouse 12.8.2 (mobile + desktop) against a local static server (no CDN, no compression — so absolute numbers are conservative and Vercel-specific behaviours are flagged PENDING); asset-weight inventory; font strategy; render-blocking resources; third-party scripts; caching config.
LAB RESULTS (local, throttled mobile): homepage Performance 90 / A11y 100 / BP 96 / SEO 100; FCP 2.0 s, LCP 2.7 s, TBT 170 ms, CLS 0, SI 4.1 s, total 259 KiB. Desktop: 100 / 100 / 96 / 100; LCP 0.6 s. Video page mobile: 89; LCP 2.2 s; CLS 0.203.

[SPEED] [SEVERITY: Medium] [VERIFIED] — Mobile LCP 2.7 s is almost entirely render delay (TTFB 0.46 s, load 0, render delay 2.28 s): the LCP element is the inline hero SVG `blueprint-hero.svg` and the browser spends 1.1 s in Style & Layout before painting it, driven by 46.7 KB of inline CSS + 46.2 KB of inline JS in a 156 KB document with 691 DOM nodes; Lighthouse flags 17 KB unused CSS, 9 KB CSS + 7 KB JS minification savings, and a forced reflow in the inline script — evidence: lh-home-mobile.json (largest-contentful-paint-element phases; mainthread-work-breakdown; unused-css-rules; unminified-*; forced-reflow-insight) — fix: (1) minify inline CSS/JS at commit time (add a tiny `tools/minify.mjs` step or move CSS/JS to external cached files); (2) split FYSP/method-state CSS so only the hero's critical CSS is inline; (3) fix the forced reflow (read layout before writing in the trace animation) — target LCP ≤ 2.0 s lab.
[SPEED] [SEVERITY: Medium] [VERIFIED] — Video page CLS 0.203 (fails the 0.1 threshold): the page declares Poppins with `font-display: swap` but preloads no fonts (0 `rel="preload"` vs 3 on the homepage), so the header wordmark/sub reflow on font arrival and shift `.eyebrow`, `h1`, `.lede` and the player (`.stage`) — evidence: lh-video-mobile.json CLS 0.203; Playwright layout-shift observer: shift 0.157 with sources DIV.eyebrow, H1, P.lede, DIV.stage; `grep -c preload making-the-pieces-fit.html` = 0 — fix: preload poppins-400/600/800 on both video pages, add a metric-matched fallback (`size-adjust`/`ascent-override`) or use `font-display: optional` for the header.
[SPEED] [SEVERITY: Medium] [VERIFIED] — Video poster JPGs are 1920×1080 at ~183 KB each with no WebP/AVIF alternative; Lighthouse estimates 120–172 KiB savings on the video page — evidence: lh-video-mobile.json modern-image-formats / image-delivery-insight; poster dimensions script — fix: generate 1280×720 WebP (≈40 KB) via the existing `tools/build-posters.py`, keep JPG as `<picture>` fallback, and reference the WebP in `og:image` only if the social platforms in use accept it (LinkedIn does not → keep JPG for OG).
[SPEED] [SEVERITY: Low] [VERIFIED] — Homepage declares and downloads 10 Poppins files (~82 KB): weights 300–800 plus italic 300/400/500/600, while weight 300 is used 3× and italics 11×; only 3 are preloaded — evidence: `grep url('/fonts/` index.html; total-byte-weight list — fix: drop 300/300-italic/500-italic/600-italic unless the design needs them (≈34 KB), or subset with `unicode-range`; keep the 3 preloads.
[SPEED] [SEVERITY: Low] [VERIFIED] — One render-blocking external stylesheet (`/website-assets/blueprint/method/method-states.css`, 4.2 KB) and no `preconnect` for `cdn.usefathom.com`, `cdn.vercel-insights.com` or `i.ytimg.com` — evidence: lh render-blocking-resources; `grep preconnect index.html` = 0 — fix: inline method-states.css (already small) or load it with `media="print" onload`; add `<link rel="preconnect">` for the two analytics hosts and `dns-prefetch` for i.ytimg.com.
[SPEED] [SEVERITY: Low] [VERIFIED] — No cache policy is declared: vercel.json has no `headers` block, so fonts, SVGs and posters rely on Vercel defaults (short max-age, ETag revalidation) instead of immutable long-lived caching — evidence: `grep -c headers vercel.json` = 0 — fix: add `headers` for `/fonts/(.*)`, `/website-assets/(.*)`, `/assets/(.*)` with `Cache-Control: public, max-age=31536000, immutable` (filenames are stable and versioned by deploy).
[SPEED] [SEVERITY: — ] [PENDING VERIFICATION] — Field Core Web Vitals (CrUX) unavailable: PSI API 429 quota, CrUX API 403 (needs key), live domain egress-blocked. Vercel's gzip/brotli, HTTP/2, edge TTFB and cache headers therefore unobserved; Lighthouse's "enable text compression 120 KiB" and "bf-cache no-store" flags are artefacts of the local `http-server -c-1` and are NOT recorded as findings — evidence: psi.json error body — fix: run PSI from any browser (pagespeed.web.dev) and paste the mobile CWV panel; if CrUX has no data yet (likely at current traffic), rely on lab numbers above.
[SPEED] [SEVERITY: Info] [VERIFIED] — Positives: self-hosted fonts with `font-display: swap`; 3 font preloads on the homepage; every image has width/height (CLS 0 on the homepage); LCP image has `fetchpriority="high"` and is not lazy-loaded; below-fold YouTube thumbs are `loading="lazy"`; analytics scripts are `defer`; no YouTube iframes until click (`pim: no player before click`); 18 MB MP4 uses `preload="metadata"`; third-party main-thread cost 0 ms; DOM 691 nodes.
REFLECT: Lab metrics, asset inventory, fonts, caching, third-parties and CLS root cause all verified; only field data is missing and that is a stated tool gap. Verified.

### Loop 4 — Mobile usability (2026-09-10)
PLAN: Viewport meta on every page; horizontal overflow, tap-target size, legible font size, nav pattern and reduced-motion at 360/375/768 px via Playwright; Lighthouse mobile SEO/accessibility audits; the repo's own `npm run verify` suite (axe, overflow, keyboard, touch) as corroboration.

[MOBILE] [SEVERITY: Info] [VERIFIED] — Homepage mobile fundamentals pass: viewport meta present; 0 px horizontal overflow at 360, 375, 768, 1280 and 1920; hamburger `nav-toggle` with `aria-expanded` and label "Menu" below 960 px; body text 17 px, h1 36 px at 375; 0 of 67 interactive targets under 24 px; Lighthouse mobile: font-size 99.3 % legible, target-size pass, viewport pass, accessibility 100; `npm run verify` PASS (axe 0 violations on index at 4 widths, CLS 0.005, reduced-motion honoured, FYSP stacked correctly on mobile) — evidence: scratchpad/mobile-check output; lh-home-mobile.json; verify.log — fix: none.
[MOBILE] [SEVERITY: Medium] [VERIFIED] — making-the-pieces-fit.html has a large layout shift on mobile (CLS 0.203, culprit `<main>`) because the `<video>` sits in a container without a reserved aspect ratio, so the poster pushes content down when it arrives — evidence: lh-video-mobile.json `layout-shifts` (1 shift, `<main>`), `cumulative-layout-shift` 0.203 — fix: add `aspect-ratio: 16 / 9; width: 100%` (or explicit width/height attributes) to the `<video>`/wrapper so space is reserved before the poster loads.
[MOBILE] [SEVERITY: Low] [VERIFIED] — On the video pages only 63 % of text is ≥12 px (Lighthouse `font-size` on making-the-pieces-fit.html), and the two footer links (site URL) render under 24 px tall at 375 px — evidence: lh-video-mobile.json font-size displayValue "63.07% legible text"; mobile-check `smallTargetSample` on /making-the-pieces-fit.html — fix: raise caption/footer text to ≥14 px and give footer links `padding-block` for a ≥24 px hit area.
[MOBILE] [SEVERITY: Low] [VERIFIED] — privacy.html: 26–40 inline table-of-contents and in-text links render under 24 px tall at 360–768 px (Lighthouse exempts inline text links, so it still scores pass, but stacked TOC links are easy to mis-tap) — evidence: mobile-check `smallTargets` 30 (375), 26 (360), 40 (768) on /privacy.html — fix: style the TOC as a block list with ≥8 px vertical padding per item.
[MOBILE] [SEVERITY: Low] [VERIFIED] — axe "region" (moderate) on privacy.html, terms.html and puzzler_card.html: content outside landmark regions (`section` on legal pages, `#toast` on the card) — evidence: verify.log notes — fix: wrap page content in `<main>` and the toast in an `aria-live` region inside a landmark.
[MOBILE] [SEVERITY: Low] [PENDING VERIFICATION] — Real-device rendering (iOS Safari, Android Chrome) and Google's Mobile Usability / Page Experience report cannot be checked here — evidence: none — fix: open Search Console → Page Experience once the property exists; spot-check on two physical devices.
REFLECT: Checked every published page at three mobile/tablet widths, both automated suites and Lighthouse. Nothing left that a specialist could reach without device access. Verified.


### Loop 10 — Live HTTP-layer verification (2026-09-11)
PLAN: The user added the site to the audit environment's network allowlist after verifying the domain in Search Console and submitting the sitemap. Re-run every check that was PENDING for lack of live access. The allowlist entry covers `www.puzzlerconsultingadvisory.com` only; the bare domain still returns a proxy 403, so apex-side behaviour is inferred from the www host, which serves the same Vercel deployment.
RESULTS (all observed with curl on 2026-09-11):
- Live homepage is byte-identical to `origin/main` index.html (168,643 bytes both; `cmp` equal). The source-based method used on 2026-09-10 is confirmed sound.
- `http://www…/` → 308 → `https://www…/`. HSTS present: `strict-transport-security: max-age=63072000` (no includeSubDomains, no preload).
- `https://www…/` → **200**, no redirect to the apex. Content-encoding br, HTTP/2, TTFB ≈0.16 s, HTML transfer 40,990 bytes (169 KB uncompressed).
- `/google9e9afb59d0e9643d.html` → 200 with the exact token (Search Console verification confirmed by the user).
- `/robots.txt` and `/sitemap.xml` → 200, identical to repo; sitemap served as application/xml.
- `/this-page-does-not-exist-seo-probe` → 404 (Vercel plain-text default; confirms the missing custom 404 page).
- `/making-the-pieces-fit` and `.html` both 200; `/capability-brief` and `.html` both 200 (duplicate confirmed). `/about` → 404 while `/about.html` → 200 (dormant canonical-to-404 confirmed). `/puzzler_card.html` → 200. `/index.html` → 200 with no redirect to `/`.
- Asset `Cache-Control` on fonts, hero SVG, brand SVG and poster JPG: `public, max-age=0, must-revalidate` (no long-lived caching; confirms the cache-policy finding).
- Live `DRAFT COPY` markers: 16 (was 14 on 2026-09-10; PRs #25/#26 added copy). Live JSON-LD blocks: 0.

APEX RESULTS (added later on 2026-09-11 once the bare domain was allowlisted): `https://puzzlerconsultingadvisory.com/` → **307** → `https://www.puzzlerconsultingadvisory.com/`; the same 307-to-www applies to `/robots.txt`, `/google9e9afb59d0e9643d.html` and unknown paths (the apex 404 is a 307 first). `http://` on the apex still can't be observed (proxy denies plain-HTTP CONNECT). So `www` is the primary domain in Vercel and the apex is a redirecting alias — the opposite of what the site's own metadata assumes.

[TECHNICAL] [SEVERITY: High] [VERIFIED] — Canonical host mismatch: every `<link rel="canonical">`, every `og:url`, all three sitemap `<loc>` entries and the robots.txt `Sitemap:` line name `https://puzzlerconsultingadvisory.com/…`, but that host 307-redirects every path to `https://www.puzzlerconsultingadvisory.com/…`, which serves the content with a 200. The declared canonical URLs are therefore all redirects; Search Console will report the sitemap URLs as "Page with redirect", and a 307 (temporary) gives Google the weakest possible consolidation signal — evidence: `curl -I https://puzzlerconsultingadvisory.com/` = HTTP/2 307, `location: https://www.puzzlerconsultingadvisory.com/`; `curl -I https://www.…/` = 200; canonical tags and sitemap.xml = apex — fix (recommended, no code change): Vercel → Project → Settings → Domains → set `puzzlerconsultingadvisory.com` as the primary domain and configure `www` as "Redirect to primary (308)". That makes every existing canonical, og:url and sitemap entry correct in one step. Alternative: keep `www` primary and rewrite canonicals, og:url, sitemap.xml and the robots.txt Sitemap line to the www host. Either way, re-verify with `curl -I` on both hosts and make sure the Search Console property is a *Domain* property (covers both hosts) rather than a single URL-prefix.
[TECHNICAL] [SEVERITY: Info] [VERIFIED] — Supersedes the earlier Loop 10 note that "www never redirects to the apex": that observation was correct but incomplete; the real defect is the direction of the redirect versus the direction the metadata declares (above).
[TECHNICAL] [SEVERITY: Low] [VERIFIED] — `/index.html` is a second live copy of the homepage (200, no redirect) — evidence: `curl -o /dev/null -w '%{http_code}' …/index.html` = 200, 0 redirects — fix: add `{ "source": "/index.html", "destination": "/", "permanent": true }` to vercel.json `redirects`.
[TECHNICAL] [SEVERITY: Info] [VERIFIED] — Status change: the Loop 1 "HTTPS enforcement / HSTS / 404 status" item moves from PENDING to VERIFIED (all good, see results above). The Loop 3 "Vercel compression / HTTP-2 / TTFB" portion moves to VERIFIED (Brotli, HTTP/2, ≈160 ms). Field Core Web Vitals remain PENDING (needs CrUX/PSI data).
[OFF-PAGE] [SEVERITY: Info] [VERIFIED] — Search Console property verified and sitemap submitted by the site owner on 2026-09-11 (reported by the user; the verification file is confirmed live). Coverage / indexation data typically takes several days to appear; the Loop 1 "absent from index" finding stays open until then.
REFLECT: Everything that was blocked on live HTTP access has now been observed directly on both hosts; only third-party hosts (social profiles, Calendly, HeyGen) remain unreachable. Verified.


### Loop 11 — Resolutions confirmed live (2026-09-11, later)
- Vercel domain settings changed by the owner: bare domain now connected to Production; `www` set to "308 Permanent Redirect" to the bare domain. Verified with curl: `https://puzzlerconsultingadvisory.com/` → 200 (body byte-identical to `main`); `https://www…/` → 308 → bare domain; `http://www…/` → 308 → https → 308 → bare domain; verification file, robots.txt, sitemap.xml and `/making-the-pieces-fit` all 200 on the bare domain; unknown path 404. HSTS present on both hosts.
- **Loop 10 High finding "Canonical host mismatch" → RESOLVED.** Every canonical, og:url and sitemap entry now names the host that serves the content, and www consolidates with a permanent redirect. No code change was needed.
- Search Console: owner added the DNS TXT record (`google-site-verification=amlKusDAHXgvAMMRxSgIe_sUql-i34HQWlCyTyudGLQ`, confirmed propagated at 8.8.8.8 and 1.1.1.1 alongside the existing SPF record), verified a **Domain property** covering both hosts, and submitted sitemap.xml on it. The earlier URL-prefix verification via the HTML file remains in place.
- Still open from Loop 1: whether the homepage is indexed (High). Check Search Console → Pages and URL Inspection on `/` from ~2026-09-15.


### Loop 12 — "Now" roadmap items implemented (2026-09-11)
Implemented in one PR (site files, tooling and this record):
- robots.txt: three `Disallow` lines removed so the dormant pages' `noindex,nofollow` can be read.
- sitemap.xml: now generated by `tools/build-sitemap.mjs` (`npm run build:sitemap`): five URLs (home, both video clean URLs, privacy, terms) with `lastmod` from git.
- vercel.json: 301 redirects `/index.html` → `/`, `/making-the-pieces-fit.html` → `/making-the-pieces-fit`, `/capability-brief.html` → `/capability-brief`; `Cache-Control` headers: fonts immutable for a year; `/website-assets/`, `/assets/` and root images one week with stale-while-revalidate (not immutable, because those filenames are not content-hashed and are edited in place).
- Homepage: `<title>` now "Nonprofit Capacity, Compliance & Leadership Advisory | Puzzler Consulting & Advisory Services"; meta description rewritten to 141 chars with audience, service, location and CTA; `twitter:title` aligned with `og:title`; preconnect to the two analytics hosts and dns-prefetch for i.ytimg.com; JSON-LD `ProfessionalService` + `Person` + `WebSite` graph; footer gains a full NAP line (`<address>`) and links to both video pages. The `<h1>` tagline was left unchanged: it is a locked brand decision (Brand Standards / handoff §4) and needs the founder's call.
- Video pages: self-canonical to the clean URL, `og:url`, Twitter card tags, `og:image` dimensions, `og:video` + type on the MP4 page; three font preloads (400/700/800) to remove the CLS; `VideoObject` JSON-LD (duration PT54S and uploadDate 2026-06-17 from the file and git); MP4 poster switched to a 1280×720 WebP (14 KB vs 183 KB) generated by `tools/build-video-posters.py`, JPG kept for `og:image`; footer gains NAP and links to home, the other video, privacy and terms.
- Legal pages: footer gains home + both video links and the NAP line.
- New `404.html` (noindex, brand palette from the homepage tokens, links to home, both videos, Fit Call); added to html-validate and check-assets lists.
- Correction to Loop 2: the "h2 → h5 skip" finding was a false positive from scanning the inline script text; in the rendered DOM the result card follows the Step 2 `<h4>`, so `<h5>` is valid. Withdrawn; markup unchanged.
- Owner decisions received 2026-09-11: the three dormant pages (about/services/contact) are deleted; `puzzler_card.html` is now `noindex, nofollow` (canonical removed). The h1 was rewritten on the owner's instruction: "Strategic Consulting for Nonprofits and Small/Medium Businesses — Built to Hold." The locked tagline "Making the pieces fit." stays as the display line above it (now a `<p class="hero-tagline">`). Meta description aligned (149 chars).
Verification: `npm run check:assets` pass; `npm run validate` pass (7 files incl. 404.html). Lighthouse 12.8 (local, mobile) before → after: making-the-pieces-fit.html Performance 89 → 100, CLS 0.203 → 0.005, LCP 2.2 s → 1.3 s, page weight 228 → 66 KiB; homepage Performance 90 → 94, Speed Index 4.1 s → 2.7 s, TBT 170 → 0 ms, CLS 0, SEO 100 on both (canonical audit now passes on the video page). `npm run verify` result recorded in the PR.

### Loop 5 — On-page SEO (2026-09-10)
PLAN: Title/description length and keyword presence on all pages; H1/H2 wording vs. buyer search intent; keyword density for the services actually offered; image alt; OG/Twitter consistency; duplicate titles.

[ON-PAGE] [SEVERITY: High] [VERIFIED] — Homepage `<title>` (63 chars) and `<h1>` ("Making the pieces fit.") contain no service, audience or location term; the eight `<h2>`s are brand phrases ("Build It to Hold", "The Puzzler Method", "Pieces in Motion", "Capable, Competitive, and Resilient"). The body never uses the phrases buyers search: "nonprofit consulting" 0, "grant compliance" 0, "fractional COO" 0, "strategic plan" 0, "Annapolis"/"Maryland" 0 — while "capacity building" (17) and "leadership development" (10) are strong — evidence: Loop 5 keyword scan of index.html body; H2 list — fix: keep the tagline as the visual hero but move it to a `<p class="kicker">`; make the h1 descriptive, e.g. "Nonprofit capacity, compliance, and leadership advisory — practitioner-led"; retitle to "Nonprofit & Small-Business Advisory in Annapolis, MD | Puzzler Consulting & Advisory Services"; add one plain-language keyword to each h2 kicker (e.g. "Who we serve: nonprofit capacity building").
[ON-PAGE] [SEVERITY: Medium] [VERIFIED] — Homepage meta description is 368 characters (three sentences); Google truncates at ~155–160, so the government-contracting sentence (added 2026-09-10) never shows in a snippet. capability-brief.html (189) and services.html (176) also exceed the limit — evidence: description length scan — fix: rewrite to ≤155 chars leading with audience + service + location, e.g. "Practitioner-led capacity, compliance, government-contracting and leadership advisory for nonprofits and small businesses. Book a 30-minute Fit Call."
[ON-PAGE] [SEVERITY: Medium] [VERIFIED] — Video pages are thin: making-the-pieces-fit.html has 84 words of body text, capability-brief.html 109, puzzler_card.html 85; none carries a transcript or summary, so the video content is invisible to search — evidence: per-page word counts — fix: add a 150–300-word summary plus a collapsed transcript under each player, and VideoObject schema (Loop 6).
[ON-PAGE] [SEVERITY: Low] [VERIFIED] — `og:description`/`twitter:description` differ from the meta description on the homepage (three different sentences), which is fine, but `twitter:title` drops the tagline; and the three YouTube thumbnail `<img>`s use `alt=""` inside links whose only name is an `aria-label` — acceptable for accessibility, but the images carry no indexable alt — evidence: index.html OG/Twitter block; thumbnail markup — fix: give thumbnails descriptive alt (film title + "poster") and align twitter:title with the page title.
[ON-PAGE] [SEVERITY: Info] [VERIFIED] — Positives: unique title and description on every page; single h1 per page; heading order valid except the h5 noted in Loop 2; every `<img>` has an alt attribute (decorative ones empty); Lighthouse SEO 100 on all three audited pages; `lang="en"`; no keyword stuffing; the founder bio (238 words) and speaking-topics list are strong E-E-A-T raw material.
REFLECT: Checked titles, descriptions, H-tags, keyword usage, alts, OG/Twitter across all nine files. Verified.

### Loop 6 — Structured data / schema markup (2026-09-10)
PLAN: Search every HTML file for JSON-LD, Microdata or RDFa; assess which schema types the content supports.

[SCHEMA] [SEVERITY: High] [VERIFIED] — Zero structured data anywhere: 0 `application/ld+json` blocks, 0 `itemscope`/`itemprop` across all 9 HTML files (the "structured data" phrase in IMPLEMENTATION_STATUS.md refers to the FYSP data model, not schema.org) — evidence: `grep -c ld+json *.html` = 0 everywhere; `grep -E 'item(scope|prop)'` = 0 — fix: add JSON-LD on the homepage for `ProfessionalService` (name, url, logo, telephone +1-410-970-6539, email, address 1125 West Street Suite 200 #348 Annapolis MD 21401, areaServed, sameAs = LinkedIn/YouTube/TikTok/Threads, founder → Person) and `Person` (Mark D. Williams, jobTitle, knowsAbout, sameAs); `WebSite`; on the two video pages `VideoObject` (name, description, thumbnailUrl, uploadDate, contentUrl/embedUrl, duration). Optional: `Service` items for the five practices once they have their own URLs, and `Event`/`speakingEvent`-style `Person.performerIn` when talks are scheduled.
REFLECT: Exhaustive grep across all files; nothing to miss. Verified.

### Loop 7 — Content quality & gaps (2026-09-10)
PLAN: Assess depth, originality, E-E-A-T signals, trust content, local signals, freshness, draft state, and which buyer questions have no page.

[CONTENT] [SEVERITY: High] [VERIFIED] — Fourteen `<!-- DRAFT COPY -->` blocks are live on the production homepage (Operator Gap heading and bridge, outcomes intro and pillar lines, Method intro and seven step descriptions, FYSP "how Puzzler can help" bullets, Who-We-Serve heading, Ways-to-Begin heading) — evidence: `grep -c 'DRAFT COPY' index.html` = 14; IMPLEMENTATION_STATUS.md line 210 — fix: founder sign-off pass; remove the markers as each block is approved.
[CONTENT] [SEVERITY: High] [VERIFIED] — No proof content: 0 testimonials, 0 case studies, 0 named client outcomes, no credentials page, no press/speaking history; the only trust signals are the founder's first-person note and a LinkedIn link — evidence: keyword scan ("testimonial" 0, "case stud" 0); section inventory — fix: add a "Work" or "Results" section with 2–3 anonymised engagement stories (situation → what Puzzler did → outcome), a credentials strip, and speaking history; these also become the sameAs/award fields in schema.
[CONTENT] [SEVERITY: Medium] [VERIFIED] — No local-business signals on any indexable page: the Annapolis, MD address appears in Releases.md and the legal docs' history but not in index.html, privacy.html or terms.html ("annapolis" = 0 on all); phone and email are present — evidence: NAP scan — fix: add the full NAP to the site footer (all pages), match it exactly in a Google Business Profile and the LinkedIn page, and mark it up in the ProfessionalService schema.
[CONTENT] [SEVERITY: Medium] [VERIFIED] — No content engine: no blog, resources, FAQ, or downloadable guides; the site has 3 indexable content pages and the newest non-legal content is the homepage itself. Topics already written in-house (seven Method steps, eight "starting points", ten government-contracting capabilities, six speaking topics) are natural standalone articles — evidence: file inventory; sitemap — fix: start with 4 evergreen pieces mapped to FYSP paths (e.g. "Preparing a nonprofit for its first government contract", "What a subrecipient monitoring review actually checks"), one per month, each linking to its practice section.
[CONTENT] [SEVERITY: Low] [VERIFIED] — The "Pieces in Motion" section (three creative-studio videos: The Falcon and the Shadow, LOCK, QUIET MIND) is topically unrelated to advisory services and sits on the same URL, diluting the page's topical focus — evidence: index.html `#pieces-in-motion`; H3s — fix: move to `/studio/` (or the YouTube channel) with a single homepage teaser, or clearly frame it as "digital storytelling" services with a link to that practice.
[CONTENT] [SEVERITY: Info] [VERIFIED] — Positives: writing is original, specific and practitioner-voiced; the FYSP tool (56 verified need×audience combinations) is genuinely differentiated; ethics boundary language is explicit; legal pages are complete (privacy 5,496 words, terms 1,300).
REFLECT: Depth, proof, locality, freshness, focus and draft state all examined. Verified.

### Loop 8 — Off-page signals (2026-09-10)
PLAN: Check brand/URL presence in web search; attempt to resolve linked social profiles; backlink/DA tools.

[OFF-PAGE] [SEVERITY: High] [PENDING VERIFICATION] — No search visibility for the brand: `site:` query = 0 results; exact-name query and "Puzzler Consulting Mark Williams Annapolis" return only unrelated "Puzzle(s) Consulting" firms and other Mark Williamses; the LinkedIn company URL does not surface in search — evidence: four WebSearch queries 2026-09-10 — fix: (1) GSC verification + sitemap submit; (2) Google Business Profile; (3) LinkedIn company page "Website" field pointing at the domain; (4) 5–10 foundational citations (Maryland Nonprofits consultant directory, SAM.gov/state vendor listings if applicable, Annapolis chamber, Clutch/GoodFirms). Cause is pending: could be non-indexation (Loop 1) rather than absence of mentions.
[OFF-PAGE] [SEVERITY: — ] [BLOCKED] — Backlink profile, referring domains, domain authority and brand-mention monitoring need Ahrefs/Semrush/Moz/GSC "Links" report; none available in this session. Social profile URLs (LinkedIn, YouTube, TikTok, Threads), Calendly and HeyGen embed could not be resolved (egress blocked, CODE=000) so their existence/consistency is unverified — evidence: curl results — fix: export GSC Links report or run one Ahrefs Site Explorer pass; confirm all four social handles resolve and use the same name/logo/URL.
[OFF-PAGE] [SEVERITY: Low] [VERIFIED] — Brand-name ambiguity: "Puzzler" collides with Puzzler Media (puzzler.com/business.puzzler.com) and several "Puzzle(s) Consulting" firms in search; the full legal name "Puzzler Consulting & Advisory Services" is the only unambiguous handle and it is absent from the h1 — evidence: search results — fix: use the full name in h1/title/schema and social bios consistently.
REFLECT: Everything obtainable without external tools was done; remaining items need tool/credential access. Marked blocked with reasons.

### Loop 9 — Competitive positioning (2026-09-10)
PLAN: No competitors supplied. Record the visible SERP for two representative target queries as context only.

[COMPETITIVE] [SEVERITY: — ] [BLOCKED] — No competitor list provided; no rank-tracking tool. Context only (PENDING VERIFICATION, single search each): "nonprofit capacity building consultant Maryland" → Maryland Nonprofits consulting group, PAGE Capacity Builders (Hunt Valley, with per-town landing pages), nonprofit.ist directory; "government contracting readiness consultant small business nonprofit" → BDO, Cherry Bekaert, CMG Alliance, SGCR, Complete Contract Consulting. Both SERPs are dominated by sites with dedicated service/location pages, reinforcing the Loop 2 architecture finding — evidence: WebSearch 2026-09-10 — fix: supply 3–5 named competitors and target keywords to run a proper gap analysis.
REFLECT: Blocked for stated reason; partial context logged, not asserted as fact.


## Open Questions / Blocked Items
- NEEDS HUMAN: competitor names (3–5) and priority keywords/geographies, to run Loop 9 properly.
- NEEDS HUMAN: backlink tool export (Ahrefs/Semrush/Moz) or GSC Links report for Loop 8.
- RESOLVED 2026-09-11: puzzler_card.html set to noindex, nofollow (QR-only destination).
- RESOLVED 2026-09-11: about.html, services.html and contact.html deleted. Their canonical-to-404 and robots/noindex conflicts go with them.
- RESOLVED 2026-09-11: homepage `<h1>` rewritten to a descriptive heading on the owner's instruction; tagline retained visually.
- RESOLVED 2026-09-11: both hosts were added to the allowlist and all HTTP-layer checks were run live (Loop 10, including the apex results). Only plain-HTTP on the apex remains unobservable (the proxy denies non-TLS CONNECT), which is immaterial because HSTS is set.
- BLOCKED (quota): Google PageSpeed Insights API returned HTTP 429 (daily quota exhausted for the shared unauthenticated project); CrUX API returned 403 (needs API key). Field Core Web Vitals are PENDING VERIFICATION; lab data comes from local Lighthouse instead.
- IN PROGRESS: Search Console Domain property verified via DNS TXT and sitemap submitted 2026-09-11. Re-check Pages and URL Inspection on `/` from 2026-09-15 to confirm indexation and close the Loop 1 High finding.

## Iteration Count: 12
